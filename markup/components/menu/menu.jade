mixin menu(items)
	- var modiffer = attributes.class || '';
	.menu(class="#{modiffer}")
		.menu__mobile
		.menu__list
			each item, i in items
				- if(i == page_id)
					.menu__item._active
						a(href="#" title="")= item
				- else		
					.menu__item
						a(href="#" title="")= item				

